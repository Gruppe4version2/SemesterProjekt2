<Page
    x:Class="VisionGroup.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:VisionGroup.Viewmodels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:ProjectViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <NavigationView
                        OpenPaneLength="200">
            <NavigationView.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </NavigationView.Background>
            <NavigationView.MenuItems>
                <NavigationViewItem Style="{StaticResource NavigationViewItemStyle}" Icon="Find" Content="Projekt søgning" />
                <NavigationViewItem Style="{StaticResource NavigationViewItemStyle}" Icon="Add" Content="Opret nyt projekt" />
                <NavigationViewItemSeparator/>
                <NavigationViewItem Style="{StaticResource NavigationViewItemStyle}" Icon="AlignLeft" Content="Projekter" />
                <NavigationViewItem Style="{StaticResource NavigationViewItemStyle}" Icon="People" Content="Firmaer/kunder" />
            </NavigationView.MenuItems>

            <Frame x:Name="Frame" HorizontalAlignment="Stretch" Margin="0,-56,0,0" VerticalAlignment="Stretch">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
    </Grid>
</Page>
